__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/

# Fabric snapshots — run data, not repository data
snapshots/
*.snapshot.json

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
tfplan
crash.log

# Backend configuration (module 4, PART C). Addresses would be harmless, but
# this is the file people paste a personal access token into — so it never
# gets the chance to be committed. The example stays.
backend.hcl
!backend.hcl.example

# NEVER commit: the Vault token
.vault-token
.pytest_cache/
